# 任务：前端放开到 19 级，并验证小路名真的能显示出来

## 背景

`/home/longshao/project/zhian-map`，React 19 + Leaflet + Vite，Docker Nginx 跑 5007。
底图是独立服务 `zhian-tiles` :5099。

**动手前先完整读完**：`/home/longshao/.agents/skills/zhian-map/SKILL.md`

刚刚下完了全市 19 级瓦片（3,251,556 张，100%，0 坏图）。下它的**唯一目的**是：
高德把"西三路"这类社区级小路名**排在 19 级才画出来**，18 级只有店铺 POI、路是光的。

**服务端已经完全就绪，别去动它**：
- `curl http://127.0.0.1:5099/tiles/gaode/19/425449/227791.png` → `200 image/png 7829B`
- zhian-tiles 容器是 bind mount，19 级目录实时可见，**不需要重启容器**
- 它的 nginx.conf 没有任何层级限制

瓶颈只在前端把层级锁死在 18。

## 要改的（就两个数字）

1. `src/App.tsx:267` —— `L.map(...)` 的 `maxZoom: 18` → `19`
2. `src/App.tsx:307` —— 高精度图层 `highZoomLayer` 的 `maxZoom: 18` → `19`

顺手把跟着的注释改对：
- 第 305 行 `// 2. 云城区市中心高精度瓦片图层 (14-18 级)` → `(14-19 级)`
- 第 309 行 `// 全市 14-18 级` → `// 全市 14-19 级`

## 千万别动的（这是陷阱）

- **`src/App.tsx:604` 的 `maxZoom: 15` 绝对不要改**。那是 `L.heatLayer` 的参数，
  含义是"热力强度达到峰值的缩放级"，跟瓦片层级毫无关系。改了会让热力图显示错乱。
- `lowZoomLayer` 的 `minZoom:12 / maxZoom:13`（293–294 行）不要动。
- `minZoom: 12` 不要动。
- 不要动坐标转换 `wgs84ToGcj02` / `gcj02ToWgs84`、统计口径、`map-config.json`、
  `nginx.conf`、`zhian-tiles` 的任何东西。

## 验证（这是本次任务的重点，必须做到）

### 验证目标：在页面上亲眼看到"西三路"三个字

已经帮你把坐标算好了（来自真实警情记录，已做 WGS84→GCJ02 纠偏）：

- 库里 WGS84：`112.127789, 22.951139`
- 地图上要用的 GCJ02：**`112.133144, 22.948541`**
- 对应 19 级瓦片：`x=425449 y=227791`（`tiles/gaode/19/425449/227791.png`，本地已存在）

参考：直接看这张瓦片图能确认"西三路"就在附近——19 级的 3×3 拼图里，
"西三路"三个字是竖排显示的。18 级同一块地则**没有**这个路名。

### 怎么让 playwright 定位到那里

`map` 是组件内的局部变量，页面上取不到。**验证期间**临时在 `src/App.tsx`
创建完 map 之后加一行把它暴露出来：

```ts
(window as any).__map = map;   // 临时调试用
```

然后 playwright 里 `await page.evaluate(() => (window as any).__map.setView([22.948541, 112.133144], 19))`
（注意 Leaflet 的 setView 是 **[纬度, 经度]**，别写反）。

**验证做完必须把这行删掉再做最终构建**，不要把调试钩子留进生产镜像。
（也就是说要构建两次：带钩子的用来截图验证，删掉后再正式构建部署。）

### 必须交的截图（放 scratchpad，绝对路径写进报告）

1. `z19_xisanlu.png` —— 缩放到 **19 级**、定位到上面那个坐标，**画面里能看清"西三路"三个字**。
   这张是本次任务的验收核心，看不到就是没做成。
2. `z18_xisanlu_compare.png` —— 同一位置**18 级**，用于对比（这张应该看不到路名）。
3. `z19_fullpage_1920.png` —— 19 级下的整页截图，确认排版没垮、故障红条没误弹。

### 命令行验证

```bash
# 前端产物里 maxZoom 确实变成 19 了
curl -s http://127.0.0.1:5007/ | grep -o 'assets/index-[^"]*\.js' 
curl -s "http://127.0.0.1:5007/<上面的js>" | grep -o 'maxZoom:19' | head -3

# 19 级瓦片走 5099 通
curl -s -o /dev/null -w "%{http_code} %{content_type}\n" http://127.0.0.1:5099/tiles/gaode/19/425449/227791.png

# 页面与底图服务都活着
curl -s -o /dev/null -w "5007:%{http_code} " http://127.0.0.1:5007/
curl -s -o /dev/null -w "5099:%{http_code}\n" http://127.0.0.1:5099/healthz
```

### 回归检查

- 12–18 级各挑一级看一眼，底图正常、没有裂图、没有误弹的故障红条；
- 1920×1080 和 1366×768 两个尺寸排版无回归（前几轮刚修好的，别弄坏）；
- `npm run build` 通过；`npx oxlint` 保持 6 warning / 0 error（基线，不许新增）。

## 收尾

改完重新构建并部署，照 `zhian-map` skill 里写的三步走：

```bash
npm run build
docker build -t yf-map:latest .
docker stop yf-map-system && docker rm yf-map-system
docker run ...        # 挂载参数照抄 deploy.sh，三个 json 一个都不能少
docker save yf-map:latest -o yf-map-latest.tar    # 别漏这步
```

**注意**：这次只重打 `yf-map` 前端镜像。瓦片摆渡包（`zhian-tiles` 那边的
`gaode-yunfu-7-18.tar`）**这次不用管**，那是下一个任务的事。

## 硬约束

- 不要 `git commit`，不要 `push`，改完停下等审查。
- 不要动 `zhian-tiles` 容器（5099），那是共享底图服务，别 stop 它。
- 不要改 `~/.agents/skills/` 下的任何 SKILL.md（要等用户点头，不归你）。
- playwright 装独立 venv，别污染系统 Python。

## 报告

1. 改了哪几行（贴改动前后）
2. 上面每条命令的实际输出
3. 三张截图的绝对路径，并说明第 1 张里"西三路"在画面什么位置
4. 确认调试钩子 `__map` 已从最终构建里删除（给出证据，比如在产物里 grep 不到）
5. 发现但没改的问题
