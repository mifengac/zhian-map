# 任务：修复 zhian-map 底图不显示 + 整改排版

## 背景

项目 `/home/longshao/project/zhian-map`，React 19 + Leaflet + Vite，Docker Nginx 暴露 **5007**。
底图**不属于本项目**，走独立服务 `zhian-tiles` **:5099**。

**动手前必须完整读完这两份 skill，按它写的口径做：**
- `/home/longshao/.agents/skills/zhian-map/SKILL.md`
- `/home/longshao/.agents/skills/zhian-tiles/SKILL.md`

交流用中文。

---

## 任务一：底图不显示

### 根因（已定位，不要再重新排查，直接修）

1. 容器 `yf-map-system` 跑的 `yf-map:latest` 镜像是 **8 周前**构建的，里面的 JS 把瓦片地址硬编码成
   `./tiles/gaode/{z}/{x}/{y}.png`（容器内 `grep -c "map-config" /usr/share/nginx/html/assets/*.js` = **0**）。
2. 8 月 26 日提交 `984c7bb` 已把 `src/App.tsx` 改成运行时读 `map-config.json` 指向 5099，
   **但镜像和 `dist/` 从未重建**。本地 `dist/` 也是 7 月 5 日的旧产物。
3. `nginx.conf` 里 `/tiles/` 的 location 已按设计删除，于是旧 JS 向 5007 要瓦片全部 404，
   代码里 `tileerror` 又用透明 1×1 GIF 兜底 → 不报错、不裂图，地图一片黑。
4. **5099 本身完全正常**，已实测：`/healthz` → 200；`/tiles/gaode/12/3311/1775.png` → 200 image/png 7260 字节。

### 要做的事

**1. 先补 `.dockerignore`（关键，不补别 build）**

项目根目录**当前没有 `.dockerignore`**，而 `Dockerfile` 是 `COPY . .`。
`tiles/` 下有几十万张 PNG（几个 GB），还有 26MB 的 `yf-map-latest.tar`。
直接 build 会把构建上下文撑爆。新建 `/home/longshao/project/zhian-map/.dockerignore`，至少排除：

```
tiles/
*.tar
node_modules/
dist/
__pycache__/
.git/
*.png
*.log
screenshot.png
incidents.json 保留（构建需要）——注意别误排
```

自己核对 `Dockerfile` 和 `public/` 里构建真正需要的文件，别把要用的排掉。
补完先验证：`docker build` 打印的 `transferring context` 应当 **< 50MB**。

**2. 重建镜像并重启容器**

```bash
cd /home/longshao/project/zhian-map
docker build -t yf-map:latest .
docker stop yf-map-system && docker rm yf-map-system
# 挂载参数照抄 deploy.sh 里的 docker run，一个都不能少：
#   -p 5007:80
#   -v $(pwd)/incidents.json:/usr/share/nginx/html/incidents.json
#   -v $(pwd)/case_type_config.json:/usr/share/nginx/html/case_type_config.json
#   -v $(pwd)/map-config.json:/usr/share/nginx/html/map-config.json
#   --restart always
```

**3. 补健壮性（skill 明确要求，就是这条缺失让问题被掩盖了整整一周）**

`src/App.tsx` 约 200–212 行，现在 `fetch('./map-config.json')` 失败或 `tileUrl` 不合法时是**静默**
退回相对路径 `./tiles/gaode/...`。按 `zhian-tiles` skill 的要求改成：

- `console.error` 打出具体原因；
- **页面上给可见提示**：「底图未配置，或 zhian-tiles(:5099) 未启动」，不要静默空白；
- 再给 `tileerror` 加一个失败计数，短时间内连续失败超过阈值（比如 20 张）时，弹同样的提示条。
  提示条样式跟现有暗色主题一致，可关闭，不要挡住地图操作。

**4. 明确不要做的**

- **不要**把 `tiles/` 挂进 `yf-map` 容器；
- **不要**在 5007 的 `nginx.conf` 里恢复 `/tiles/` location；
- **不要**把瓦片 URL 或 IP 写死进源码；
- **不要**动 `zhian-tiles` 容器和它的 nginx 配置（那边是好的）。

### 验收（每条都要跑，把实际输出贴进报告）

```bash
# a. 构建上下文体积（build 日志里的 transferring context）应 < 50MB
# b. 新 JS 里必须能 grep 到 map-config
curl -s http://127.0.0.1:5007/ | grep -o 'assets/index-[^"]*\.js'
curl -s http://127.0.0.1:5007/assets/<上面拿到的文件名> | grep -c "map-config"   # 期望 > 0
# c. 5099 瓦片仍然通
curl -s -o /dev/null -w "%{http_code} %{content_type}\n" http://127.0.0.1:5099/tiles/gaode/12/3311/1775.png
# d. 浏览器截图证明底图真的画出来了（见任务二的 playwright）
# e. 故障提示验证：docker stop zhian-tiles → 刷新页面 → 截图里能看到提示文字
#    验完务必 docker start zhian-tiles 起回来！
```

---

## 任务二：整改排版

### 先截图存证

用 playwright 装到**独立 venv**（`python3 -m venv`，别用 `pip install --break-system-packages` 污染系统 Python）。
改前、改后各截两个尺寸：**1920×1080** 和 **1366×768**，共 4 张，存到
`/tmp/claude-1000/-home-longshao-project-zhian-map/8501eed8-441a-4841-bf67-3a9080ea9054/scratchpad/`，
文件名带 before/after 和尺寸。报告里写清路径。

### 已确认的硬伤（按重要性排序）

**1. 时间轴卡片被白扣 800px（最要命）**

`src/App.css:722` `.timeline-axis-card` 写着 `left: 400px; right: 400px;`，
按"左栏 380 + 右栏 380"硬算的。但这个卡片在 `<main className="map-container">` **内部**
（`App.tsx` 1078 行，`<main>` 是 1020–1241 行），`position:absolute` 是相对**地图容器**定位的，
而地图容器本来就已经排在两个侧栏之间了 —— 等于又往里缩了 800px。

后果：1920 屏上时间轴只剩 ~360px 宽，控件挤成一坨；1366 屏上 `1366-760-800 < 0`，直接塌掉。

改成相对地图容器定位，例如 `left: 20px; right: 20px;`，或者居中 + `max-width`。

**2. 整个 `App.css` 零个 `@media`**

左右两个 `aside` 各写死 `width: 380px`（`App.css:35`、`App.css:47`），没有任何响应式。
加断点，建议：
- `≤1600px`：侧栏收窄到 320px；
- `≤1280px`：右侧详情面板 `.right-panel` 改成可收起 / 覆盖式浮层，把地图让出来。

**3. `.map-overlay-tools`（`App.css:357`）**

6 个按钮一行 flex，没有 `flex-wrap`，窄屏会溢出地图区。加 `flex-wrap: wrap` 和合理的 `max-width`。

**4. 左下角"离线底图模式"说明条**

`App.tsx:1144` 起，一大坨内联 style，且 `bottom:20px` 和时间轴卡片同高，视觉上打架。
抽成 CSS class，位置和时间轴错开，或者干脆并进时间轴卡片里。

**5. 满地内联 style 覆盖 CSS**

`App.tsx` 的 751、775、843、1119、1276 等行有大量纯样式的内联 `style={{...}}`，
和 `App.css` 打架，是排版乱的根源之一。把**纯静态样式**抽进 `App.css`，
只保留真正依赖 state 的动态值（比如进度条的 `width: ${percent}%`、`flex: selectedIncident ? ...`）。

### 约束

- **只改样式和布局**。不改任何业务逻辑、数据处理、坐标转换（`wgs84ToGcj02` / `gcj02ToWgs84`）、统计口径。
- 配色和暗色主题风格**保持不变**，不要重新设计视觉，不要换字体。
- **不引入任何新依赖**（不上 Tailwind、不上 UI 库）。playwright 只装在 venv 里做截图，不进 `package.json`。
- `index.html` 里引的 Google Fonts 是外网 CDN，内网打不开——**顺手记下来写进报告**，但这次先别动。

### 验收

- `npm run build` 通过；
- `npx oxlint` 不新增报错（跑之前先记下基线数量）；
- 1920×1080 和 1366×768 两个尺寸下：**无横向滚动条、无元素溢出、时间轴卡片宽度合理、按钮不换行错位**；
- 改完重新 `docker build` + 重启容器，截图取自 5007 的实际页面，不是 vite dev；
- 4 张对比截图路径写进报告。

---

## 最终报告要求

1. 改了哪些文件，每个文件改了什么（一句话一处，不要贴大段代码）；
2. 上面每条验收命令的**实际输出**；
3. 4 张截图的绝对路径；
4. 过程中发现但**没改**的问题（比如 Google Fonts 那条），单独列一节。

不要 git commit，不要 push，改完就停，等人审查。
