# 任务：把 433 万张瓦片打成单个 PMTiles，服务端内部改读它，对外契约一个字不变

## 背景

- 瓦片目录：`/home/longshao/project/zhian-map/tiles/gaode/`，层级 **7–19**，
  共 **4,338,146 张 PNG**，占盘 **18 GB**（内容约 2.5 GB，小文件占块严重）。
- 底图服务：`/home/longshao/project/zhian-tiles`，Docker 跑 nginx，端口 **5099**，
  瓦片由宿主机 bind mount 进容器（软链指向 zhian-map/tiles）。

**动手前必须完整读完这两份 skill：**
- `/home/longshao/.agents/skills/zhian-tiles/SKILL.md`
- `/home/longshao/.agents/skills/zhian-map/SKILL.md`

### 为什么要做

433 万个小文件过网闸是灾难：光 `tar` 一遍就要很久，内网落地还要解压出 433 万个文件。
打成 PMTiles 后是**单个文件**，过闸和落地都是秒级。

抽样 3000 张实测：**内容重复率 75.5%，空白瓦片（179 字节）占 76%**，
最高频的那一个内容出现了 2217 次。PMTiles 天生对相同内容的瓦片只存一份，
所以转换后体积还会明显缩水。

## 最重要的约束：对外契约一个字都不能变

`http://<IP>:5099/tiles/gaode/{z}/{x}/{y}.png` 这个契约**不止 zhian-map 在用**，
`zhian-wurenji` 也指着它，`intranet-projects` skill 里记了端口表。

所以**绝对不许**改成"前端直读 PMTiles"——那样每个消费方的前端都得改。

正确做法：**只换服务端内部实现**，让 5099 继续用完全相同的 URL 吐出完全相同的 PNG 字节。
做完之后：
- `zhian-map` 前端**一行都不用改**
- `map-config.json` 不用改
- `nginx.conf` 里 `/tiles/` 的对外行为不变
- `/healthz` 照旧
- CORS 头照旧（`Access-Control-Allow-Origin: *`）

## 分两步做，每步都要能独立验证

### 第一步：目录 → 单个 PMTiles

写转换脚本 `/home/longshao/project/zhian-tiles/build_pmtiles.py`。

技术选型（已替你验证过可用）：Python `pmtiles` 库，装在**独立 venv**里，
别污染系统 Python。关键 API：

```python
from pmtiles.writer import Writer
from pmtiles.tile import zxy_to_tileid
```

要点：

1. **必须按 tileid 升序写入**。tileid 是全局递增的（z 小的更小），
   所以按 z 从小到大、每层内部按 tileid 排序即可保证全局有序。
2. **注意内存**。433 万个瓦片，别把"路径字符串"全放进内存排序；
   只存 `(tileid, x, y)` 三元组，路径按需拼接。19 级单层就有 325 万个，
   请实测内存占用并在报告里写明峰值。
3. **元数据要写对**：
   - `tile_type` = PNG
   - `min_zoom` = 7，`max_zoom` = 19
   - bounds = `111.05, 22.36, 112.52, 23.32`（云浮全市，和下载框一致）
   - center 用 `112.0444, 22.9298`（zhian-map 里的市中心）
   - **坐标系是 GCJ-02**，这点务必写进 metadata 的说明字段，
     以免以后有人拿它当 WGS-84 用（这是本项目最容易踩的坑）。
4. **跑得久**：读 433 万个小文件，预计 1–3 小时。**必须后台跑**
   （`nohup setsid ... &`），带进度日志，别占着前台。
5. **原目录不许删**。转换完 `tiles/gaode/` 原样保留，作为回退和以后补图的源。
   磁盘够（剩 798G）。

### 第二步：5099 内部改读 PMTiles

`zhian-tiles` 现在是纯 nginx 出静态 PNG。nginx 读不了 PMTiles，需要一个能
"解 PMTiles → 出 XYZ PNG"的服务。

`/home/longshao/project/zhian-tiles/serve.py` 已经有一个 Python 版底图服务
（契约与 nginx.conf 相同，本来是"无 nginx 镜像时的备选"）。**建议在它基础上改**，
用 `pmtiles` 库的 Reader，好处是不引入新的二进制依赖，离线部署最简单。

如果你评估下来 Python 性能不够（内网就几个大屏客户端，瓦片还有 30 天浏览器缓存，
按理够用），可以改用 go-pmtiles 单静态二进制 + nginx 反代，但**必须在报告里
给出实测数据说明为什么**，不要凭感觉换方案。

要求：
- 路径、状态码、Content-Type、CORS 头、缓存头，全部与现在的 nginx 行为一致；
- `/healthz` 返回 200 `ok`；
- 找不到的瓦片返回 **404**（不许像之前 yf-map 那样回退成 200 HTML，
  那个坑刚填过，别再挖一遍）；
- PMTiles 的目录树要在启动时加载并常驻内存，不要每个请求都重新解析。

## 验证（每条都要有实际输出）

### A. 字节级一致性（最关键）

随机抽 **200 张**（覆盖 7–19 各层级，其中 19 级至少 100 张），
比对"PMTiles 服务吐出来的字节"和"原 PNG 文件"的 **md5 是否完全相同**。
必须 200/200 一致，有一张不一致就是转换有问题。

特别要抽到这张（西三路，19 级，本次下载的验收点）：
`tiles/gaode/19/425449/227791.png`

### B. 契约兼容性

```bash
curl -s -o /dev/null -w "%{http_code} %{content_type} %{size_download}\n" \
  http://127.0.0.1:5099/tiles/gaode/19/425449/227791.png     # 期望 200 image/png 7829
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:5099/healthz   # 200
curl -sI http://127.0.0.1:5099/tiles/gaode/19/425449/227791.png | grep -i "access-control\|cache-control\|content-type"
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:5099/tiles/gaode/19/1/1.png  # 不存在的应 404
```

### C. 前端零改动验证（证明契约真的没变）

**不许改 zhian-map 的任何代码**，直接用现在跑着的 5007 页面：
- 用 playwright（独立 venv）截图 19 级西三路那个位置
  （GCJ02 `112.133144, 22.948541`，zoom 19），**画面里必须能看到"西三路"三个字**；
- 再截 12 级全市图，确认低层级也正常；
- 存到 scratchpad，路径写进报告。

参考：`zhian-map` 前端的 map 是组件内局部变量，playwright 定位需要临时钩子，
但**这次不许改 zhian-map 源码**——请改用鼠标滚轮/点击缩放按钮的方式操作到位，
或者直接比对瓦片请求日志确认层级正确。

### D. 体积与性能

- PMTiles 文件大小 vs 原目录 18GB 占盘、2.5GB 内容，写明去重省了多少；
- 单张瓦片响应耗时（`curl -w "%{time_total}"`，取 20 次平均）；
- 并发 20 拉 200 张的总耗时（模拟一个大屏同时要图）。

### E. 服务稳定性

服务要能被 systemd 或 docker 正常拉起、重启后自动恢复。给出你的部署方式，
以及**内网怎么部署**的具体步骤（写进 `zhian-tiles/离线部署.md`）。

## 硬约束

- **不许改 `zhian-map` 的任何源码**（这是"契约没变"的证明手段）。
- **不许删 `tiles/gaode/` 原目录**。
- 不要 `git commit`，不要 `push`，改完停下等审查。
- 5007（yf-map）容器不要动。
- 转换脚本后台跑，别让这一轮卡死在等待上。
- 网络：先直连，失败再加 `http://127.0.0.1:7897` 代理，不要写进任何持久化配置。

## 报告

1. PMTiles 文件路径、大小、与原目录的体积对比（去重省了多少）
2. 转换耗时、内存峰值
3. A/B/C/D/E 每项的实际输出
4. 截图绝对路径
5. 服务改造前后对比（贴关键代码/配置）
6. 内网部署步骤
7. 发现但没改的问题
